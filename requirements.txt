# core
pandas
openpyxl
faker

# xlsx engines (для .xls и .xlsb)
xlrd
pyxlsb

# matching — TF-IDF (без этого упадёт на difflib, медленнее в 10-100x)
scikit-learn
numpy

# matching — rapidfuzz (запасной вариант, быстрее difflib)
rapidfuzz

# matching — русская морфология (лемматизация токенов для гейтов)
pymorphy3

# transliterate (кириллица → латиница для TF-IDF, опционально)
transliterate

# dagster
dagster
dagster-webserver

# kafka
kafka-python