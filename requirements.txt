# ── Ядро (обязательно) ───────────────────────────────
pandas
openpyxl
dagster
dagster-webserver
kafka-python

# ── Опционально (расширяют возможности) ──────────────
faker          # качественная генерация синтетических имён (ru_RU)
scikit-learn   # лучший fuzzy-матчинг товаров (TF-IDF)
rapidfuzz      # альтернативный fuzzy-матчинг (если нет sklearn)
xlrd           # поддержка старого формата .xls
pyxlsb         # поддержка формата .xlsb
